<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Login</title>
    <link rel="stylesheet" href="styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<style>
     /* General page styling */
     
       
    /* Basic resets and styling */
* {
margin: 0;
padding: 0;
box-sizing: border-box;
}
body, html {
    height: 100%;
    font-family: 'Poppins', sans-serif;
    background-image: url('/images/loginbg.jpg'); /* Adjust this path */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    justify-content: center;
    align-items: center;
}



.login-container {
    background: rgba(0, 0, 0, 0.7); /* Slight transparent black background */
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
    width: 100%;
    max-width: 400px; /* Responsive width */
}

.login-header {
text-align: center;
margin-bottom: 2.5rem;
}

.login-header h2 {
font-size: 2rem;
color: white;/* Deep blue color for header */
}

input[type="email"],
input[type="password"] {
width: 100%;
padding: 1rem;
margin-bottom: 1.5rem;
border: 1px solid #b0b5c3;
border-radius: 8px;
font-size: 1rem;
color: #2b3f73;
background-color: #f5f6fb;
transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

input:focus {
border-color: #4f75db;
box-shadow: 0 0 10px rgba(79, 117, 219, 0.5); /* Subtle glow on focus */
outline: none;
}

button {
width: 100%;
padding: 1rem;
background-color: #4f75db; /* Bright primary color */
color: white;
font-size: 1.2rem;
font-weight: 600;
border: none;
border-radius: 8px;
cursor: pointer;
transition: background-color 0.3s ease, box-shadow 0.3s ease;
position: relative;
overflow: hidden;
}

button:hover {
background-color: #3b5bbf;
box-shadow: 0 0 20px rgba(79, 117, 219, 0.7); /* Glowing effect */
}

button:after {
content: '';
position: absolute;
top: -100%;
left: -100%;
width: 300%;
height: 300%;
background: radial-gradient(circle, rgba(79,117,219,0.5), transparent 70%);
opacity: 0;
transition: opacity 0.4s, transform 0.4s;
}

button:hover:after {
opacity: 1;
transform: translate(50%, 50%);
}

.forgot-password {
display: block;
text-align: right;
margin-top: 1rem;
color:white;
text-decoration: none;
font-size: 0.9rem;
}

.forgot-password:hover {
text-decoration: underline;
}

.signup-link {
text-align: center;
margin-top: 1.5rem;
font-size: 0.9rem;
}

.signup-link a {
color: white;
text-decoration: none;
font-weight: 500;
}

.signup-link a:hover {
text-decoration: underline;
}

/* Media queries for responsiveness */
@media (max-width: 768px) {
.login-container {
padding: 1.5rem;
max-width: 90%;
}
}
body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            height: 100vh;
        }

        /* Heading styling */
        h1 {
            font-size: 3rem;
            font-weight: 600;
            text-align: center;
            background: linear-gradient(90deg, #ff6a00, #ee0979);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            margin-top: 2rem; /* Top margin to push down from the top */
            letter-spacing: 2px;
        }

        /* Glow effect */
        h1:hover {
            animation: glow 1.5s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from {
                text-shadow: 2px 2px 10px rgba(255, 106, 0, 0.7), 2px 2px 20px rgba(238, 9, 121, 0.7);
            }
            to {
                text-shadow: 2px 2px 15px rgba(255, 106, 0, 1), 2px 2px 30px rgba(238, 9, 121, 1);
            }
        }

</style>
</head>
<body background="../public/images/loginbg.JPEG">
    <h1>The Book Store</h1>
    
    <div class="login-container">
        <div class="login-header">
            <h2>Login</h2>
        </div>
        
        <form action="/do/login" method="post">
            <input type="email" placeholder="Email Address" name="emailId" required>
            <input type="password" placeholder="Password" name="password" required>
            <button type="submit">Login</button>
            <a href="#" class="forgot-password">Forgot Password?</a>
        </form>
        
        <div class="signup-link">
            <p>Don’t have an account? <a href="/signup">Sign up here</a></p>
        </div>
    </div>

</body>
</html>
